resources:
  - ../../base

patches:
  - target:
      kind: Rollout
      name: node-k8s-app
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2 # Reduzido de 10 para 2 para caber no cluster econômico
